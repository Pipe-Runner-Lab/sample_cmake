# minimum required version of the cmake tool
cmake_minimum_required( VERSION 3.0 )

# complier 

# output - executable name followed by all the source files we wish to compile
add_executable(
	output
	main.cpp
)